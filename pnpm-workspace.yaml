onlyBuiltDependencies:
  - "@parcel/watcher"
  - "@swc/core"
  - esbuild
  - sharp
  - unrs-resolver

# The Sanity CLI's framework detector pins vulnerable parser versions.
# Keep overrides within the same major; remove when upstream updates its pins.
overrides:
  "@vercel/frameworks>js-yaml": 3.15.1
  "@vercel/frameworks>smol-toml": 1.6.1
